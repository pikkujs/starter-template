// Wire global HTTP middleware here. Telemetry, auth, request logging, etc.
// are injected by the deploy bundle — keep this file focused on the
// middleware your app actually owns.
//
// Example:
//   import { addHTTPMiddleware } from '@pikku/core/http'
//   addHTTPMiddleware('*', [yourMiddleware])
export {}
