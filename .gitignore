node_modules
dist
.vite
.next
.tanstack
.output
*.tsbuildinfo

# Pikku build/codegen outputs — regenerated by `pikku all` and the deploy bundler.
.pikku
.deploy

# Yarn (template is structure only — users run their own install)
yarn.lock
.yarn/
.pnp.*

# Env
.env
.env.local
